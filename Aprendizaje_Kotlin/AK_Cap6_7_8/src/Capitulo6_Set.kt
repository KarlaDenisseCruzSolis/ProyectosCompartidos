
fun main() {
// Crea un nuevo conjunto (Set) de solo lectura que contiene elementos de tipo Int
    val set = setOf(1, 3, 5)

// Imprime el conjunto en la consola. La salida será: "[1, 3, 5]"
    println(set)
}
